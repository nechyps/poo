# Инструкция по установке и использованию базы данных для игры Тамагочи

## Описание

В игру Тамагочи добавлена поддержка базы данных SQLite для сохранения и загрузки состояния питомца. База данных работает в браузере с использованием `sql.js` и сохраняется в IndexedDB.

## Архитектура

### Модель данных

Таблица `pets` содержит следующую структуру:

```sql
CREATE TABLE pets (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL DEFAULT 'Tamagotchi',
  hunger REAL NOT NULL DEFAULT 80,        -- Уровень сытости (0-100)
  happiness REAL NOT NULL DEFAULT 80,     -- Уровень счастья (0-100)
  energy REAL NOT NULL DEFAULT 80,        -- Уровень энергии (0-100)
  cleanliness REAL NOT NULL DEFAULT 80,  -- Уровень чистоты (0-100)
  health REAL NOT NULL DEFAULT 100,       -- Уровень здоровья (0-100)
  coins INTEGER NOT NULL DEFAULT 0,      -- Количество монет
  last_updated INTEGER NOT NULL,          -- Время последнего обновления (timestamp)
  created_at INTEGER NOT NULL,            -- Время создания (timestamp)
  session_id TEXT UNIQUE                 -- Уникальный ID сессии
)
```

### Слой доступа к данным

Файл `src/db/petRepository.js` содержит следующие функции:

- `createPet(petData)` - создание нового питомца
- `getPetById(petId)` - получение питомца по ID
- `getPetBySessionId(sessionId)` - получение питомца по session_id
- `updatePetStats(petId, stats)` - обновление статистики питомца
- `getAllPets()` - получение всех питомцев (для отладки)
- `deletePet(petId)` - удаление питомца

### Хуки React

- `usePet()` - основной хук для работы с питомцем в БД
  - Автоматически загружает питомца при старте
  - Предоставляет функции `manualSave()` и `manualLoad()`
  - Отслеживает время последнего сохранения

- `useStats()` - обновлен для работы с БД
  - Загружает статистику из БД при старте
  - Автоматически сохраняет изменения в БД
  - Сохраняет изменения немедленно при действиях игрока

- `useCoins()` - обновлен для работы с БД
  - Загружает количество монет из БД
  - Автоматически сохраняет изменения монет в БД

## Установка зависимостей

Все необходимые зависимости уже установлены:

```bash
npm install sql.js knex idb-keyval
```

### Зависимости:

- `sql.js` - SQLite, скомпилированный в JavaScript для работы в браузере
- `knex` - SQL query builder (установлен, но в текущей реализации используется напрямую sql.js)
- `idb-keyval` - удобная обертка над IndexedDB для хранения бинарных данных БД

## Инициализация базы данных

База данных инициализируется автоматически при первом использовании хука `usePet()`. 

Процесс инициализации:
1. Загружается библиотека sql.js
2. Пытается загрузить существующую БД из IndexedDB
3. Если БД не найдена - создается новая с таблицей `pets`
4. Проверяется версия БД и выполняются миграции при необходимости

## Использование

### Автоматическое сохранение

Игра автоматически сохраняет состояние в следующих случаях:
- При изменении статистики (сытость, счастье, энергия и т.д.)
- При изменении количества монет
- При выполнении действий (покормить, поиграть, уложить спать)
- Каждые 30 секунд (автосохранение)

### Ручное сохранение и загрузка

В меню игры (кнопка бургер-меню) добавлены кнопки:
- **💾 Save** - ручное сохранение игры
- **📂 Load** - ручная загрузка игры

Также отображается время последнего сохранения в формате:
- "Только что" - если сохранено менее минуты назад
- "X мин. назад" - если сохранено менее часа назад
- "X ч. назад" - если сохранено менее суток назад
- "X дн. назад" - если сохранено менее недели назад
- Дата и время - для более старых сохранений

## Обработка ошибок

Все операции с БД обернуты в try-catch блоки с логированием ошибок в консоль. При ошибках:

1. Ошибка логируется в консоль браузера
2. Пользователю показывается сообщение об ошибке (если возможно)
3. Игра продолжает работать (fallback на локальное состояние)

### Типичные ошибки:

- **Ошибка инициализации БД** - может возникнуть при первой загрузке, если не удалось загрузить sql.js
- **Ошибка сохранения** - может возникнуть при переполнении IndexedDB или проблемах с браузером
- **Ошибка загрузки** - может возникнуть при повреждении данных в IndexedDB

## Миграции базы данных

При изменении схемы БД необходимо:

1. Увеличить `CURRENT_DB_VERSION` в `src/db/database.js`
2. Добавить логику миграции в функцию `checkAndMigrate()`

Пример:

```javascript
async function checkAndMigrate() {
  const currentVersion = await get(DB_VERSION_KEY) || 0
  
  if (currentVersion < CURRENT_DB_VERSION) {
    if (currentVersion < 2) {
      // Миграция на версию 2
      dbInstance.run('ALTER TABLE pets ADD COLUMN new_field TEXT')
    }
    
    await set(DB_VERSION_KEY, CURRENT_DB_VERSION)
    await saveDatabase()
  }
}
```

## Отладка

Для отладки можно использовать функции из `petRepository.js`:

```javascript
import { getAllPets, deletePet } from './db/petRepository'

// Получить всех питомцев
const pets = await getAllPets()
console.log('Все питомцы:', pets)

// Удалить питомца (для тестирования)
await deletePet(petId)
```

## Замечания

1. **Браузерное хранилище**: БД хранится в IndexedDB браузера, поэтому данные привязаны к конкретному браузеру и устройству.

2. **Размер БД**: SQLite в браузере имеет ограничения по размеру. Для игры Тамагочи это не проблема, так как данные минимальны.

3. **Производительность**: Все операции с БД асинхронные и не блокируют UI. Сохранение происходит с небольшой задержкой (debounce) для оптимизации.

4. **Совместимость**: Требует поддержки IndexedDB и WebAssembly (для sql.js). Поддерживается во всех современных браузерах.

## Запуск игры

```bash
npm run dev
```

Игра автоматически инициализирует БД при первом запуске.

## Структура файлов

```
src/
├── db/
│   ├── database.js          # Инициализация и управление БД
│   └── petRepository.js     # Функции доступа к данным
├── hooks/
│   ├── usePet.js            # Хук для работы с питомцем в БД
│   ├── useStats.js          # Хук статистики (обновлен для БД)
│   └── useCoins.js          # Хук монет (обновлен для БД)
└── components/
    ├── Game/
    │   └── Game.jsx          # Основной компонент игры (обновлен)
    └── Menu/
        ├── Menu.jsx          # Меню с кнопками save/load (обновлено)
        └── Menu.css          # Стили меню (обновлены)
```

## Дополнительная информация

Для работы с БД на сервере (Node.js) можно использовать `better-sqlite3` или `sqlite3` с `knex`. Текущая реализация использует `sql.js` для работы в браузере, но архитектура позволяет легко адаптировать код для серверной части.

