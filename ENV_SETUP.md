# Настройка переменных окружения (.env)

## 📝 Создание файла .env

Для корректной работы приложения необходимо создать файл `.env` в корневой директории проекта.

### Шаг 1: Создайте файл
В корне вашего проекта (`C:\web-development\poo\`) создайте новый файл с именем `.env` (без расширения).

### Шаг 2: Добавьте переменные
Скопируйте и вставьте следующее содержимое в файл `.env`:

```env
# Supabase Configuration
VITE_SUPABASE_URL=https://lrzlsmbyrzfjaevcmggr.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImxyemxzbWJ5cnpmamFldmNtZ2dyIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjM2NTAwMzgsImV4cCI6MjA3OTIyNjAzOH0.jP1ysi_SxKSzVDnwmvyfEKCHqH940mFuzaV4V02dYkM
```

### Шаг 3: Сохраните файл
Убедитесь, что файл сохранен с именем `.env` (с точкой в начале).

---

## ⚠️ Важные замечания

1. **Безопасность:** Файл `.env` автоматически добавлен в `.gitignore` и **не будет** загружен в Git. Это правильно, так как в нем содержатся конфиденциальные данные.

2. **Vercel:** При деплое на Vercel эти переменные нужно добавить в настройках проекта (Settings → Environment Variables), а **не** через файл `.env`.

3. **Локальная разработка:** Файл `.env` нужен **только для локальной разработки** (когда вы запускаете `npm run dev` на своем компьютере).

---

## 🚀 Проверка работы

После создания файла `.env`:

1. Перезапустите dev-сервер (если он был запущен):
   ```bash
   npm run dev
   ```

2. Проверьте, что приложение успешно подключается к Supabase.

---

## ❓ Если что-то пошло не так

### Ошибка: "Supabase URL and Anon Key must be defined"
- Убедитесь, что файл `.env` создан в **корневой директории** проекта (рядом с `package.json`).
- Проверьте, что переменные называются **точно так:** `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY` (с префиксом `VITE_`).
- Перезапустите dev-сервер после создания `.env`.

### Файл .env не работает
- В Windows убедитесь, что файл не называется `.env.txt`. Отключите "Скрывать расширения для зарегистрированных типов файлов" в настройках Проводника, чтобы увидеть реальное имя файла.

