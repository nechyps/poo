# Настройка Google OAuth для localhost и Vercel

Эта инструкция поможет настроить Google OAuth так, чтобы он работал и на localhost, и на Vercel.

## 📋 Шаг 1: Настройка Supabase Dashboard

### 1.1 Откройте Supabase Dashboard
1. Перейдите на [supabase.com](https://supabase.com)
2. Войдите в свой аккаунт
3. Выберите ваш проект

### 1.2 Настройте URL Configuration
1. Перейдите в **Authentication** → **URL Configuration**
2. В разделе **Site URL** укажите ваш основной URL (Vercel):
   ```
   https://poo-86gr.vercel.app
   ```

3. В разделе **Redirect URLs** добавьте ОБА URL (каждый с новой строки):
   ```
   http://localhost:5173
   http://localhost:5173/
   https://poo-86gr.vercel.app
   https://poo-86gr.vercel.app/
   ```

   **Важно:** Добавьте оба варианта (с `/` и без), чтобы работало в любом случае.

4. Нажмите **Save**

---

## 📋 Шаг 2: Настройка Google Cloud Console

### 2.1 Откройте Google Cloud Console
1. Перейдите на [console.cloud.google.com](https://console.cloud.google.com)
2. Выберите ваш проект

### 2.2 Найдите OAuth 2.0 Client ID
1. Перейдите в **APIs & Services** → **Credentials**
2. Найдите ваш **OAuth 2.0 Client ID** (который используется для Supabase)

### 2.3 Настройте Authorized redirect URIs
В разделе **Authorized redirect URIs** должен быть ТОЛЬКО ОДИН URL от Supabase:

```
https://lrzlsmbyrzfjaevcmggr.supabase.co/auth/v1/callback
```

**Важно:** 
- ❌ НЕ добавляйте `http://localhost:5173` или `https://poo-86gr.vercel.app` напрямую
- ✅ Используйте ТОЛЬКО callback URL от Supabase
- Supabase сам обработает редирект на нужный домен на основе параметра `redirectTo`

### 2.4 Сохраните изменения
Нажмите **Save** внизу страницы

---

## 📋 Шаг 3: Проверка кода

Убедитесь, что в `src/contexts/AuthContext.jsx` используется правильный `redirectTo`:

```javascript
const currentUrl = window.location.href.split('#')[0]
const redirectTo = currentUrl
```

Этот код автоматически определит, на каком домене вы находитесь, и вернет вас туда же после авторизации.

---

## 🧪 Тестирование

### Тест на localhost:
1. Откройте `http://localhost:5173`
2. Нажмите "Войти с Google"
3. После авторизации вы должны вернуться на `http://localhost:5173`

### Тест на Vercel:
1. Откройте `https://poo-86gr.vercel.app`
2. Нажмите "Войти с Google"
3. После авторизации вы должны вернуться на `https://poo-86gr.vercel.app`

---

## 🐛 Troubleshooting

### Проблема: Все еще перекидывает на Vercel с localhost

**Решение:**
1. Проверьте, что в Supabase Dashboard в **Redirect URLs** добавлен `http://localhost:5173`
2. Убедитесь, что вы используете правильный порт (может быть 5174, 3000 и т.д.)
3. Очистите кеш браузера (Ctrl+Shift+Delete)
4. Перезапустите dev-сервер

### Проблема: Ошибка "redirect_uri_mismatch"

**Решение:**
1. Убедитесь, что в Google Cloud Console указан ТОЛЬКО Supabase callback URL
2. НЕ добавляйте localhost или Vercel напрямую в Google Cloud Console

### Проблема: Авторизация работает, но не возвращает на нужный домен

**Решение:**
1. Проверьте консоль браузера - там должен быть лог с `redirectTo`
2. Убедитесь, что в Supabase Dashboard оба URL добавлены в Redirect URLs
3. Проверьте, что Site URL в Supabase указывает на Vercel (это fallback)

---

## 📝 Важные замечания

1. **Site URL в Supabase** - это fallback URL. Если `redirectTo` не указан или не разрешен, будет использован Site URL.

2. **Redirect URLs в Supabase** - это список разрешенных URL, на которые Supabase может редиректить после авторизации.

3. **Authorized redirect URIs в Google** - это список URL, на которые Google может редиректить. Для Supabase нужен только один - callback URL от Supabase.

4. **Код автоматически определяет домен** - `window.location.href` всегда содержит текущий URL, поэтому код сам поймет, на каком домене вы находитесь.

---

## ✅ Чеклист

- [ ] В Supabase Dashboard добавлены оба URL в Redirect URLs
- [ ] Site URL в Supabase указывает на Vercel
- [ ] В Google Cloud Console указан только Supabase callback URL
- [ ] Код использует `window.location.href` для `redirectTo`
- [ ] Протестировано на localhost
- [ ] Протестировано на Vercel

