# Быстрый старт - Тамагочи с базой данных

## Установка и запуск

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Запустите игру:**
   ```bash
   npm run dev
   ```

3. **Откройте браузер:**
   Игра откроется по адресу http://localhost:5173

## Первый запуск

При первом запуске:
- База данных автоматически создастся
- Будет создан новый питомец с начальными параметрами
- Все изменения будут автоматически сохраняться

## Использование

### Автоматическое сохранение
- ✅ Сохранение происходит автоматически при каждом действии
- ✅ Автосохранение каждые 30 секунд
- ✅ Все изменения статистики сохраняются немедленно

### Ручное сохранение/загрузка

1. Откройте меню (кнопка бургер-меню в левом верхнем углу)
2. Нажмите **💾 Save** для ручного сохранения
3. Нажмите **📂 Load** для загрузки последнего сохранения
4. Время последнего сохранения отображается под кнопками

### Действия с питомцем

- **🍔 Покормить** - увеличивает сытость и счастье (стоит 20 монет)
- **🚽 Почистить** - увеличивает чистоту и счастье
- **🌙 Уложить спать** - восстанавливает энергию и здоровье
- **🏠 Дом** - вернуться домой / открыть мини-игру

### Мини-игра

1. Нажмите кнопку дома
2. Нажмите кнопку игры
3. Ловите еду для заработка монет!

## Структура базы данных

Данные питомца сохраняются в таблице `pets` со следующими полями:
- `id` - уникальный идентификатор
- `name` - имя питомца
- `hunger` - уровень сытости (0-100)
- `happiness` - уровень счастья (0-100)
- `energy` - уровень энергии (0-100)
- `cleanliness` - уровень чистоты (0-100)
- `health` - уровень здоровья (0-100)
- `coins` - количество монет
- `last_updated` - время последнего обновления
- `created_at` - время создания
- `session_id` - ID сессии (для привязки к браузеру)

## Отладка

### Просмотр данных в консоли браузера

Откройте консоль разработчика (F12) для просмотра логов:
- Инициализация БД
- Создание/загрузка питомца
- Сохранение данных
- Ошибки (если есть)

### Очистка данных

Для полного сброса игры:
1. Откройте консоль разработчика (F12)
2. Выполните:
   ```javascript
   // Очистка IndexedDB
   indexedDB.deleteDatabase('keyval-store')
   // Очистка localStorage
   localStorage.clear()
   ```
3. Перезагрузите страницу

## Решение проблем

### Ошибка загрузки sql.js
- Проверьте подключение к интернету (sql.js загружается с CDN)
- Убедитесь, что браузер поддерживает WebAssembly

### Данные не сохраняются
- Проверьте консоль браузера на наличие ошибок
- Убедитесь, что IndexedDB доступен в браузере
- Проверьте квоту хранилища браузера

### Игра не загружается
- Очистите кэш браузера
- Проверьте консоль на ошибки
- Попробуйте в режиме инкогнито

## Дополнительная информация

Подробная документация: [DATABASE_SETUP.md](./DATABASE_SETUP.md)

