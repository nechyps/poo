# Настройка Google OAuth и Supabase для игры про питомца

## 📋 Обзор

Проект использует **Supabase** для аутентификации через Google OAuth и хранения данных питомцев в облачной базе данных PostgreSQL. Все данные сохраняются в Supabase, полностью исключая использование localStorage для game state.

## 🔧 Настройка Supabase

### 1. Создание проекта в Supabase

1. Перейдите на [supabase.com](https://supabase.com)
2. Создайте новый проект или используйте существующий
3. Запишите:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **Anon Key** (публичный ключ)

### 2. Настройка Google OAuth

#### В Google Cloud Console:

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите **Google+ API**
4. Перейдите в **Credentials** → **Create Credentials** → **OAuth 2.0 Client ID**
5. Выберите тип приложения: **Web application**
6. Добавьте **Authorized redirect URIs**:
   ```
   https://YOUR_PROJECT_REF.supabase.co/auth/v1/callback
   ```
   Замените `YOUR_PROJECT_REF` на ваш Project Reference из Supabase
7. Сохраните **Client ID** и **Client Secret**

#### В Supabase Dashboard:

1. Перейдите в **Authentication** → **Providers**
2. Найдите **Google** и включите его
3. Вставьте:
   - **Client ID** (из Google Cloud Console)
   - **Client Secret** (из Google Cloud Console)
4. Сохраните изменения

### 3. Выполнение миграции базы данных

1. Откройте Supabase Dashboard
2. Перейдите в **SQL Editor**
3. Откройте файл `supabase/migrations/001_create_pet_saves.sql`
4. Скопируйте весь SQL код
5. Вставьте в SQL Editor и выполните (Run)

Это создаст:
- Таблицу `pet_saves` для хранения данных питомцев
- Row Level Security (RLS) политики для безопасности
- Индексы для оптимизации запросов

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here
```

**Важно:** 
- Замените `YOUR_PROJECT_REF` на ваш Project Reference
- Замените `your_anon_key_here` на ваш Anon Key из Supabase
- Файл `.env` уже добавлен в `.gitignore` и не будет загружен в Git

## 🏗️ Архитектура

### Структура файлов:

```
src/
├── contexts/
│   └── AuthContext.jsx          # Контекст аутентификации
├── db/
│   ├── supabaseClient.js        # Клиент Supabase
│   └── petRepositorySupabase.js # Репозиторий для работы с БД
├── hooks/
│   ├── usePetSupabase.js        # Хук для работы с питомцем
│   ├── useStats.js              # Хук статистики (обновлен)
│   └── useCoins.js              # Хук монет (обновлен)
├── components/
│   ├── Auth/
│   │   ├── AuthButton.jsx       # Кнопка входа через Google
│   │   ├── AuthScreen.jsx       # Экран авторизации
│   │   └── UserProfile.jsx     # Профиль пользователя
│   └── Menu/
│       └── Menu.jsx             # Меню (обновлено с UserProfile)
└── pages/
    └── AuthCallback.jsx         # Обработка OAuth callback
```

### Поток данных:

1. **Авторизация:**
   - Пользователь нажимает "Войти с Google"
   - Редирект на Google OAuth
   - После успешной авторизации → callback → Supabase создает сессию
   - `AuthContext` получает пользователя и сохраняет в состоянии

2. **Загрузка данных:**
   - При загрузке игры `usePetSupabase` проверяет авторизацию
   - Если авторизован → загружает данные из `pet_saves` по `user_id`
   - Если нет данных → создает нового питомца с дефолтными значениями

3. **Сохранение данных:**
   - При изменении статистики → автосохранение через 2 секунды
   - Ручное сохранение через меню
   - Все данные сохраняются в Supabase через `petRepositorySupabase`

## 🔒 Безопасность

### Row Level Security (RLS):

Таблица `pet_saves` защищена RLS политиками:
- Пользователи могут читать **только свои** сохранения
- Пользователи могут создавать/обновлять **только свои** сохранения
- Пользователи могут удалять **только свои** сохранения

### Токены:

- Supabase SDK автоматически управляет токенами
- Access tokens хранятся в памяти браузера (не в localStorage)
- Refresh tokens управляются Supabase автоматически

## 🚀 Деплой

### Vercel / Netlify:

1. Добавьте переменные окружения в настройках проекта:
   ```
   VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
   VITE_SUPABASE_ANON_KEY=your_anon_key_here
   ```

2. Убедитесь, что redirect URI в Google Cloud Console включает ваш домен:
   ```
   https://YOUR_PROJECT_REF.supabase.co/auth/v1/callback
   ```

### Локальная разработка:

1. Создайте `.env` файл (см. выше)
2. Запустите: `npm run dev`
3. Приложение будет доступно на `http://localhost:5173`

## 🧪 Тестирование

### Проверка полного флоу:

1. **Авторизация:**
   - Откройте приложение
   - Нажмите "Войти с Google"
   - Проверьте, что редирект работает
   - После авторизации проверьте, что профиль отображается в меню

2. **Создание питомца:**
   - После первого входа должен создаться новый питомец
   - Проверьте в Supabase Dashboard → Table Editor → `pet_saves`

3. **Сохранение:**
   - Выполните действия в игре (кормление, сон и т.д.)
   - Подождите 2 секунды для автосохранения
   - Или используйте ручное сохранение через меню
   - Проверьте, что данные обновились в БД

4. **Загрузка:**
   - Перезагрузите страницу
   - Проверьте, что данные восстановились

5. **Изоляция данных:**
   - Войдите под другим Google аккаунтом
   - Проверьте, что данные изолированы (новый питомец)

## 📝 Важные замечания

1. **Гостевой режим:** 
   - Приложение позволяет играть без авторизации
   - Но данные не сохраняются (только в памяти)
   - При перезагрузке данные теряются

2. **Миграция данных:**
   - Старые данные из localStorage/IndexedDB не мигрируются автоматически
   - Пользователям нужно будет начать заново после авторизации

3. **Ошибки:**
   - Все ошибки логируются в консоль
   - При ошибках БД игра продолжает работать в памяти
   - Пользователь видит предупреждения в UI

## 🐛 Troubleshooting

### Ошибка: "Supabase URL and Anon Key must be defined"
- Проверьте, что `.env` файл создан в корне проекта
- Проверьте, что переменные называются правильно (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`)
- Перезапустите dev-сервер после создания `.env`

### Ошибка: "Failed to load pet save"
- Проверьте, что миграция выполнена в Supabase
- Проверьте RLS политики в Supabase Dashboard
- Проверьте, что пользователь авторизован

### OAuth не работает
- Проверьте redirect URI в Google Cloud Console
- Убедитесь, что Google OAuth включен в Supabase
- Проверьте Client ID и Client Secret

## 📚 Дополнительные ресурсы

- [Supabase Auth Documentation](https://supabase.com/docs/guides/auth)
- [Google OAuth Setup](https://supabase.com/docs/guides/auth/social-login/auth-google)
- [Row Level Security](https://supabase.com/docs/guides/auth/row-level-security)

